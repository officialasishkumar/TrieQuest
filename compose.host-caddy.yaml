services:
  backend:
    networks:
      - app
      - host_proxy
    ports:
      - "127.0.0.1:${TRIEQUEST_BACKEND_BIND_PORT:-18000}:8000"

  caddy:
    profiles:
      - repo-caddy

networks:
  host_proxy:
